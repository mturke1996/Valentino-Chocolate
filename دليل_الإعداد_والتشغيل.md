# 🍫 دليل إعداد وتشغيل موقع بيع الشوكولاتة

## مرحباً بك! 👋

هذا الدليل الشامل سيساعدك على إعداد موقع بيع الشوكولاتة خطوة بخطوة.

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

1. **Node.js** (الإصدار 18 أو أحدث)
   - تحميل من: https://nodejs.org/
   - للتحقق: افتح Terminal واكتب `node --version`

2. **محرر أكواد** (يُفضل VS Code)
   - تحميل من: https://code.visualstudio.com/

3. **حساب Firebase** (مجاني)
   - التسجيل من: https://firebase.google.com/

4. **حساب ImgBB** (لرفع الصور - مجاني)
   - التسجيل من: https://imgbb.com/

5. **بوت تلجرام** (اختياري للإشعارات)

---

## 🚀 خطوات الإعداد

### الخطوة 1: تنصيب المكتبات

1. افتح Terminal في مجلد المشروع
2. اكتب:
```bash
npm install
```
3. انتظر حتى تنتهي عملية التنصيب (قد تستغرق بضع دقائق)

---

### الخطوة 2: إعداد Firebase

#### 2.1 إنشاء مشروع Firebase جديد

1. اذهب إلى https://console.firebase.google.com/
2. اضغط "إنشاء مشروع" أو "Add project"
3. أدخل اسم المشروع (مثلاً: chocolate-shop)
4. اضغط "متابعة"
5. (اختياري) فعّل Google Analytics
6. اضغط "إنشاء المشروع"

#### 2.2 تفعيل Authentication

1. من القائمة الجانبية، اختر "Authentication"
2. اضغط "Get Started"
3. اختر "Email/Password"
4. فعّل "Email/Password"
5. احفظ

#### 2.3 إنشاء قاعدة بيانات Firestore

1. من القائمة الجانبية، اختر "Firestore Database"
2. اضغط "Create database"
3. اختر "Start in test mode" (سنحدّث القواعد لاحقاً)
4. اختر الموقع الأقرب لك (مثلاً: europe-west)
5. اضغط "Enable"

#### 2.4 تفعيل Storage

1. من القائمة الجانبية، اختر "Storage"
2. اضغط "Get Started"
3. اختر "Start in test mode"
4. اضغط "Next" ثم "Done"

#### 2.5 الحصول على بيانات المشروع

1. من القائمة الجانبية، اختر ⚙️ (الإعدادات) → "Project settings"
2. في قسم "Your apps"، اضغط على أيقونة Web `</>`
3. أدخل اسم التطبيق (مثلاً: chocolate-shop-web)
4. (اختياري) فعّل Firebase Hosting
5. اضغط "Register app"
6. **انسخ** البيانات التي تظهر (firebaseConfig)

---

### الخطوة 3: إعداد ملف البيئة (.env)

1. في مجلد المشروع، أنشئ ملف جديد اسمه `.env`
2. افتح الملف والصق:

```env
# Firebase Configuration (ضع بياناتك هنا)
VITE_FIREBASE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXX
VITE_FIREBASE_AUTH_DOMAIN=chocolate-shop-xxxxx.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=chocolate-shop-xxxxx
VITE_FIREBASE_STORAGE_BUCKET=chocolate-shop-xxxxx.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:xxxxxxxxxxxxx

# ImgBB API (سنحصل عليه في الخطوة التالية)
VITE_IMGBB_API_KEY=your_imgbb_api_key

# Telegram Bot (اختياري - سنعده لاحقاً)
VITE_TELEGRAM_BOT_TOKEN=
VITE_TELEGRAM_CHAT_ID=

# Admin Credentials (بيانات تسجيل الدخول للأدمن)
VITE_ADMIN_EMAIL=admin@chocolate-shop.com
VITE_ADMIN_PASSWORD=password123
```

3. **مهم جداً**: استبدل القيم بقيمك الحقيقية من Firebase

---

### الخطوة 4: إعداد ImgBB (لرفع الصور)

1. اذهب إلى https://imgbb.com/
2. سجل حساب جديد أو سجل دخول
3. اذهب إلى https://api.imgbb.com/
4. اضغط "Get API key"
5. انسخ API key
6. الصقه في ملف `.env` في `VITE_IMGBB_API_KEY`

---

### الخطوة 5: رفع قواعد الأمان

#### 5.1 قواعد Firestore

1. في Firebase Console، اذهب إلى "Firestore Database"
2. اختر تبويب "Rules"
3. احذف كل المحتوى والصق:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    function isSignedIn() {
      return request.auth != null;
    }
    
    function isAdmin() {
      return isSignedIn() && 
             exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }
    
    match /products/{productId} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /orders/{orderId} {
      allow read: if isAdmin();
      allow create: if true;
      allow update, delete: if isAdmin();
    }
    
    match /settings/{settingId} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /admins/{adminId} {
      allow read: if isAdmin();
      allow write: if false;
    }
    
    match /messages/{messageId} {
      allow read: if isAdmin();
      allow create: if true;
      allow update, delete: if isAdmin();
    }
  }
}
```

4. اضغط "Publish"

#### 5.2 قواعد Storage

1. في Firebase Console، اذهب إلى "Storage"
2. اختر تبويب "Rules"
3. احذف كل المحتوى والصق:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    function isSignedIn() {
      return request.auth != null;
    }
    
    function isImage() {
      return request.resource.contentType.matches('image/.*');
    }
    
    function isValidSize() {
      return request.resource.size < 5 * 1024 * 1024; // 5MB
    }
    
    match /{allPaths=**} {
      allow read: if true;
      allow write: if isSignedIn() && isImage() && isValidSize();
    }
  }
}
```

4. اضغط "Publish"

---

### الخطوة 6: إنشاء مستخدم أدمن

#### 6.1 إنشاء المستخدم في Authentication

1. في Firebase Console، اذهب إلى "Authentication"
2. اختر تبويب "Users"
3. اضغط "Add user"
4. أدخل:
   - Email: البريد الذي وضعته في `.env` (مثلاً: admin@chocolate-shop.com)
   - Password: كلمة المرور القوية
5. اضغط "Add user"
6. **انسخ UID** (المعرف الفريد الذي يظهر في عمود "User UID")

#### 6.2 إضافة المستخدم إلى مجموعة Admins

1. اذهب إلى "Firestore Database"
2. اضغط "Start collection"
3. أدخل اسم Collection: `admins`
4. اضغط "Next"
5. في "Document ID"، الصق **UID** الذي نسخته
6. أضف الحقول التالية:

| Field Name | Type | Value |
|------------|------|-------|
| email | string | admin@chocolate-shop.com |
| displayName | string | المدير |
| role | string | super-admin |
| createdAt | timestamp | [اضغط على الساعة لتحديد الوقت الحالي] |

7. اضغط "Save"

---

### الخطوة 7: إعداد بوت التلجرام (اختياري)

إذا كنت تريد استقبال إشعارات الطلبات على التلجرام:

#### 7.1 إنشاء البوت

1. افتح تطبيق Telegram
2. ابحث عن `@BotFather`
3. ابدأ محادثة واكتب `/newbot`
4. اتبع التعليمات:
   - أدخل اسم البوت (مثلاً: Chocolate Shop Bot)
   - أدخل username للبوت (يجب أن ينتهي بـ bot، مثلاً: chocolate_shop_2024_bot)
5. **انسخ Token** الذي يظهر

#### 7.2 الحصول على Chat ID

1. ابحث عن `@userinfobot` في Telegram
2. ابدأ محادثة
3. **انسخ Your user ID**

#### 7.3 إضافة البيانات إلى .env

افتح ملف `.env` وأضف:
```env
VITE_TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
VITE_TELEGRAM_CHAT_ID=123456789
```

---

## ▶️ تشغيل المشروع

### وضع التطوير (Development)

افتح Terminal واكتب:
```bash
npm run dev
```

سيفتح الموقع على: `http://localhost:5173`

---

## 🎨 أول استخدام

### 1. تسجيل الدخول إلى لوحة التحكم

1. افتح المتصفح واذهب إلى `http://localhost:5173/admin/login`
2. أدخل البريد وكلمة المرور الذي أنشأتهما
3. اضغط "تسجيل الدخول"

### 2. رفع الشعار وصور الهيرو

1. من لوحة التحكم، اذهب إلى "الإعدادات"
2. ارفع:
   - **الشعار**: 200x200 بكسل (PNG شفاف)
   - **صور الهيرو**: 1920x800 بكسل (يمكن رفع عدة صور)
3. أدخل اسم الموقع والنصوص
4. احفظ

### 3. إضافة فئات

1. اذهب إلى "الفئات"
2. أضف فئات مثل:
   - شوكولاتة داكنة
   - شوكولاتة بالحليب
   - شوكولاتة بيضاء
   - هدايا
   - إلخ...

### 4. إضافة منتجات

1. اذهب إلى "المنتجات"
2. اضغط "إضافة منتج جديد"
3. املأ البيانات:
   - الاسم (عربي وإنجليزي)
   - الوصف
   - السعر (بالجنيه)
   - الوزن (مثلاً: 100g، 250g)
   - الفئة
4. **ارفع الصور** (800x800 بكسل - يمكن رفع عدة صور)
   - الصورة الأولى ستكون الصورة الرئيسية
   - باقي الصور ستظهر في صفحة التفاصيل
5. احفظ

---

## 📸 مقاسات الصور المطلوبة

### الشعار (Logo)
- **المقاس**: 200x200 بكسل
- **الحجم**: أقل من 100KB
- **الصيغة**: PNG شفاف

### صور الهيرو (Hero)
- **المقاس**: 1920x800 بكسل
- **الحجم**: أقل من 500KB لكل صورة
- **الصيغة**: JPG أو WebP

### صور المنتجات
- **المقاس**: 800x800 بكسل (مربع)
- **الحجم**: أقل من 300KB لكل صورة
- **الصيغة**: JPG أو WebP
- **الكمية**: يمكن رفع عدة صور لكل منتج

### صور الفئات
- **المقاس**: 400x400 بكسل
- **الحجم**: أقل من 200KB
- **الصيغة**: JPG أو WebP

**💡 نصيحة**: استخدم موقع https://tinypng.com/ لضغط الصور قبل رفعها

---

## 🌐 النشر على الإنترنت

عندما تكون جاهزاً لنشر الموقع:

### البناء للنشر

```bash
npm run build
```

سيتم إنشاء مجلد `dist` يحتوي على ملفات الموقع الجاهزة.

### النشر على Netlify (الأسهل - مجاني)

1. اذهب إلى https://netlify.com/
2. سجل حساب
3. اسحب مجلد `dist` إلى الموقع
4. انتظر حتى ينتهي الرفع
5. ستحصل على رابط مباشر للموقع!

### النشر على Firebase Hosting

```bash
npm install -g firebase-tools
firebase login
firebase init hosting
firebase deploy
```

---

## ❓ حل المشاكل الشائعة

### المشكلة: "Cannot find module..."
**الحل**: اكتب `npm install` من جديد

### المشكلة: الموقع لا يفتح
**الحل**: تأكد من أن المنفذ 5173 غير مستخدم، أو أعد تشغيل `npm run dev`

### المشكلة: لا أستطيع تسجيل الدخول
**الحل**: 
1. تأكد من إضافة المستخدم في `admins` collection
2. تأكد من صحة البريد وكلمة المرور في `.env`

### المشكلة: رفع الصور لا يعمل
**الحل**: تأكد من إضافة ImgBB API key في `.env`

### المشكلة: إشعارات التلجرام لا تصل
**الحل**: 
1. تأكد من صحة Bot Token و Chat ID
2. تأكد من بدء محادثة مع البوت أولاً

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. تحقق من Terminal للأخطاء
2. تحقق من Console المتصفح (F12)
3. راجع Firebase Console للتأكد من البيانات
4. تأكد من صحة ملف `.env`

---

## ✅ قائمة التحقق النهائية

- [ ] Node.js مثبت
- [ ] مشروع Firebase جاهز
- [ ] Authentication مفعل
- [ ] Firestore Database جاهزة
- [ ] Storage مفعل
- [ ] قواعد Firestore محدّثة
- [ ] قواعد Storage محدّثة
- [ ] ملف `.env` معبأ بالبيانات الصحيحة
- [ ] ImgBB API key جاهز
- [ ] مستخدم أدمن منشأ
- [ ] مستخدم أدمن مضاف في `admins` collection
- [ ] `npm install` تم تنفيذه بنجاح
- [ ] الموقع يعمل على localhost
- [ ] استطعت تسجيل الدخول إلى لوحة التحكم
- [ ] رفع الشعار يعمل
- [ ] إضافة منتجات يعمل

---

## 🎉 تهانينا!

موقعك جاهز الآن! استمتع ببيع الشوكولاتة 🍫

**ملاحظة نهائية**: لا تنسَ تغيير كلمة مرور الأدمن إلى كلمة قوية وآمنة قبل النشر!

