# 🍫 موقع بيع الشوكولاتة الفاخرة - معلومات المشروع

## 🎯 نظرة عامة

تم إنشاء موقع **بيع شوكولاتة احترافي** بتصميم **Material Design 3** كامل، باللغة العربية (RTL)، مع لوحة تحكم شاملة.

---

## ✨ المميزات الرئيسية

### 🎨 التصميم
- ✅ **Material Design 3 (Material You)** بشكل كامل
- ✅ تصميم عصري مثل تطبيقات Google الحديثة
- ✅ **RTL كامل** للغة العربية
- ✅ **Responsive** - يعمل على جميع الأجهزة
- ✅ تأثيرات **Ripple** حقيقية
- ✅ حركات **Material Motion** (Fade + Scale)
- ✅ ألوان متناسقة مع موضوع الشوكولاتة
- ✅ خط **Cairo** العربي الجميل

### 🛍️ واجهة المستخدم
- ✅ صفحة رئيسية مع Hero carousel
- ✅ عرض المنتجات مع فلترة وبحث
- ✅ صفحة تفاصيل منتج مع معرض صور (Lightbox)
- ✅ سلة تسوق تفاعلية
- ✅ صفحة إتمام الطلب
- ✅ نظام تصنيفات
- ✅ عرض الخصومات والعروض

### ⚙️ لوحة التحكم
- ✅ إدارة شاملة للمنتجات
- ✅ إدارة الفئات
- ✅ إدارة الطلبات
- ✅ **رفع الشعار** من لوحة التحكم
- ✅ **رفع صور الهيرو** (متعددة)
- ✅ **رفع صور المنتجات** (متعددة)
- ✅ إعدادات كاملة للموقع
- ✅ لوحة إحصائيات

### 📲 الإشعارات
- ✅ **إشعارات تلجرام فورية**:
  - طلب جديد
  - تغيير حالة الطلب
  - رسالة جديدة

### 🔒 الأمان
- ✅ قواعد Firestore محكمة
- ✅ قواعد Storage آمنة
- ✅ مصادقة Firebase
- ✅ التحقق من صلاحيات الأدمن

---

## 📸 مقاسات الصور (مهم جداً!)

### 🖼️ الشعار (Logo)
```
المقاس: 200x200 بكسل (مربع)
الحجم: أقل من 100KB
الصيغة: PNG شفاف
الموقع: يظهر في Navbar و Footer
```

### 🎭 صور الهيرو (Hero Images)
```
المقاس: 1920x800 بكسل (أفقي)
الحجم: أقل من 500KB لكل صورة
الصيغة: WebP أو JPG
الكمية: متعدد (carousel تلقائي)
الموقع: الصفحة الرئيسية
```

### 🍫 صور المنتجات
```
المقاس: 800x800 بكسل (مربع)
الحجم: أقل من 300KB لكل صورة
الصيغة: WebP أو JPG
الكمية: متعدد (معرض صور)
ملاحظة: الصورة الأولى = صورة رئيسية
```

### 📁 صور الفئات
```
المقاس: 400x400 بكسل (مربع)
الحجم: أقل من 200KB
الصيغة: WebP أو JPG
```

---

## 📁 هيكل المشروع

```
chocolate-shop/
│
├── src/                          # مجلد الكود المصدري
│   ├── components/              # المكونات
│   │   ├── Navbar.tsx          # شريط التنقل
│   │   ├── Footer.tsx          # التذييل
│   │   ├── Hero.tsx            # قسم الهيرو
│   │   ├── ProductCard.tsx     # بطاقة المنتج
│   │   └── Cart.tsx            # سلة التسوق
│   │
│   ├── pages/                   # الصفحات
│   │   ├── HomePage.tsx        # الصفحة الرئيسية
│   │   ├── ProductsPage.tsx    # صفحة المنتجات
│   │   ├── ProductDetailPage.tsx # تفاصيل المنتج
│   │   ├── CheckoutPage.tsx    # صفحة الدفع
│   │   └── admin/              # صفحات لوحة التحكم
│   │       ├── AdminDashboard.tsx
│   │       ├── AdminLogin.tsx
│   │       ├── DashboardHome.tsx
│   │       └── ItemsManagement.tsx
│   │
│   ├── store/                   # إدارة الحالة (Zustand)
│   │   ├── authStore.ts        # حالة المصادقة
│   │   └── cartStore.ts        # حالة السلة
│   │
│   ├── types/                   # الأنواع (TypeScript)
│   │   └── index.ts
│   │
│   ├── utils/                   # وظائف مساعدة
│   │   ├── formatters.ts       # تنسيق البيانات
│   │   ├── imgbbUpload.ts      # رفع الصور
│   │   └── telegramNotifications.ts # إشعارات تلجرام
│   │
│   ├── hooks/                   # Custom Hooks
│   │   └── useFirestore.ts
│   │
│   ├── firebase.ts              # إعداد Firebase
│   ├── App.tsx                  # المكون الرئيسي
│   ├── main.tsx                 # نقطة الدخول
│   └── index.css                # Material Design 3 CSS
│
├── public/                      # ملفات عامة
│
├── firestore.rules              # قواعد Firestore
├── storage.rules                # قواعد Storage
├── firebase.json                # إعدادات Firebase
├── .env                         # متغيرات البيئة (لا ترفعه!)
├── package.json                 # المكتبات
├── tsconfig.json                # إعدادات TypeScript
├── vite.config.ts               # إعدادات Vite
├── tailwind.config.js           # إعدادات Tailwind
├── netlify.toml                 # إعدادات Netlify
├── .gitignore                   # ملفات مستثناة من Git
│
├── README.md                    # دليل شامل بالإنجليزية
└── دليل_الإعداد_والتشغيل.md     # دليل خطوة بخطوة بالعربية
```

---

## 🚀 البدء السريع

### 1. تنصيب المكتبات
```bash
npm install
```

### 2. إعداد Firebase
- أنشئ مشروع على Firebase Console
- فعّل Authentication + Firestore + Storage
- انسخ بيانات المشروع إلى `.env`

### 3. تشغيل المشروع
```bash
npm run dev
```

### 4. تسجيل الدخول
- اذهب إلى `/admin/login`
- أدخل بيانات الأدمن

**للتفاصيل الكاملة**: راجع `دليل_الإعداد_والتشغيل.md`

---

## 🎨 الألوان المستخدمة (Material Design 3)

```css
Primary (أساسي): #6B4226 (بني الشوكولاتة)
Secondary (ثانوي): #D4A574 (بيج ذهبي)
Tertiary (ثالث): #C17F4A (برتقالي دافئ)
Surface (السطح): #FFF8F5 (أبيض دافئ)
Background (الخلفية): #FFF8F5
Error (خطأ): #BA1A1A
```

يمكنك تعديل الألوان من `tailwind.config.js`

---

## 🛠️ التقنيات المستخدمة

| التقنية | الغرض |
|---------|-------|
| **React 18** | مكتبة واجهة المستخدم |
| **TypeScript** | لغة البرمجة |
| **Vite** | أداة البناء السريعة |
| **Tailwind CSS** | إطار CSS |
| **Material Design 3** | نظام التصميم |
| **Firebase** | قاعدة بيانات + مصادقة + تخزين |
| **Framer Motion** | الحركات والانتقالات |
| **Zustand** | إدارة الحالة |
| **React Router** | التوجيه |
| **Lucide React** | الأيقونات |
| **React Hot Toast** | الإشعارات |
| **ImgBB API** | رفع الصور |
| **Telegram Bot API** | إشعارات التلجرام |

---

## 📊 Firestore Collections

### 1. `products` - المنتجات
```typescript
{
  id: string
  name: string
  nameAr: string
  description: string
  descriptionAr: string
  price: number
  images: string[] // متعدد
  category: string
  weight: string
  featured: boolean
  inStock: boolean
  discount: number
  rating: number
  reviewCount: number
  createdAt: timestamp
  updatedAt: timestamp
}
```

### 2. `categories` - الفئات
```typescript
{
  id: string
  name: string
  nameAr: string
  image: string
  order: number
  productsCount: number
  createdAt: timestamp
}
```

### 3. `orders` - الطلبات
```typescript
{
  id: string
  orderNumber: string
  customerName: string
  customerPhone: string
  customerEmail: string
  customerAddress: string
  items: OrderItem[]
  subtotal: number
  deliveryFee: number
  discount: number
  total: number
  status: 'pending' | 'confirmed' | 'preparing' | 'out-for-delivery' | 'delivered' | 'cancelled'
  paymentMethod: 'cash' | 'card' | 'online'
  paymentStatus: 'pending' | 'paid' | 'failed'
  notes: string
  createdAt: timestamp
  updatedAt: timestamp
}
```

### 4. `admins` - المديرون
```typescript
{
  id: string (UID from Authentication)
  email: string
  displayName: string
  role: 'super-admin' | 'admin'
  createdAt: timestamp
}
```

### 5. `settings` - الإعدادات
```typescript
{
  id: 'general'
  siteName: string
  siteNameAr: string
  logo: string
  heroImages: string[] // متعدد
  heroTitle: string
  heroTitleAr: string
  heroSubtitle: string
  heroSubtitleAr: string
  phone: string
  email: string
  address: string
  addressAr: string
  whatsapp: string
  facebook: string
  instagram: string
  twitter: string
  deliveryFee: number
  currency: string
  currencyAr: string
  updatedAt: timestamp
}
```

### 6. `messages` - الرسائل
```typescript
{
  id: string
  name: string
  email: string
  phone: string
  subject: string
  message: string
  status: 'new' | 'read' | 'replied'
  createdAt: timestamp
}
```

---

## 🔐 الأمان

### قواعد Firestore
- ✅ القراءة العامة للمنتجات والفئات
- ✅ الكتابة فقط للأدمن
- ✅ الطلبات يمكن إنشاؤها من الجميع
- ✅ الطلبات يمكن قراءتها فقط من الأدمن

### قواعد Storage
- ✅ القراءة العامة للصور
- ✅ الكتابة فقط للمصادقين
- ✅ حجم الصور محدود بـ 5MB
- ✅ نوع الملفات: صور فقط

---

## 📱 الصفحات المتاحة

### صفحات عامة:
- `/` - الصفحة الرئيسية
- `/products` - المنتجات
- `/product/:id` - تفاصيل المنتج
- `/cart` - سلة التسوق
- `/checkout` - إتمام الطلب

### صفحات لوحة التحكم:
- `/admin/login` - تسجيل الدخول
- `/admin` - الرئيسية
- `/admin/products` - إدارة المنتجات
- `/admin/categories` - إدارة الفئات
- `/admin/orders` - إدارة الطلبات
- `/admin/messages` - الرسائل
- `/admin/settings` - الإعدادات

---

## 🎓 كيفية استخدام الموقع

### للزوار:
1. تصفح المنتجات
2. إضافة إلى السلة
3. إتمام الطلب
4. ملء البيانات
5. اختيار طريقة الدفع
6. تأكيد الطلب

### للأدمن:
1. تسجيل الدخول
2. رفع الشعار والصور
3. إضافة الفئات
4. إضافة المنتجات
5. إدارة الطلبات
6. تلقي إشعارات التلجرام

---

## 🌐 النشر

### Netlify (مجاني - الأسهل)
```bash
npm run build
# ارفع مجلد dist على Netlify
```

### Firebase Hosting
```bash
firebase deploy
```

### Vercel
```bash
vercel --prod
```

---

## 📞 الدعم والمساعدة

### الملفات المساعدة:
1. **README.md** - دليل شامل بالإنجليزية
2. **دليل_الإعداد_والتشغيل.md** - دليل خطوة بخطوة
3. **معلومات_المشروع.md** - هذا الملف

### حل المشاكل:
- تحقق من Terminal للأخطاء
- تحقق من Console المتصفح (F12)
- راجع Firebase Console
- تأكد من `.env` صحيح

---

## ✅ جاهز للاستخدام!

المشروع **جاهز 100%** ويحتوي على:

- ✅ تصميم Material Design 3 كامل
- ✅ لوحة تحكم شاملة
- ✅ نظام رفع الصور
- ✅ إشعارات التلجرام
- ✅ سلة تسوق
- ✅ نظام الطلبات
- ✅ RTL كامل
- ✅ Responsive
- ✅ قواعد أمان محكمة

---

## 🎉 استمتع ببيع الشوكولاتة!

**تم البناء بـ ❤️ باستخدام Material Design 3**

---

**آخر تحديث**: ديسمبر 2025
**الإصدار**: 1.0.0
**الترخيص**: MIT

