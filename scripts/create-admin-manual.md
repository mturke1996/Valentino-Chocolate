# 🔐 دليل إنشاء مستخدم Admin يدوياً

## الطريقة السريعة (بدون سكريبت)

### الخطوة 1: إنشاء مستخدم في Firebase Authentication

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. اختر مشروع `valantino-chocolate`
3. من القائمة الجانبية، اضغط على **Authentication**
4. اضغط على علامة التبويب **Users**
5. اضغط على **Add user** (إضافة مستخدم)
6. أدخل:
   - **Email**: بريدك الإلكتروني (مثال: `admin@valentino.com`)
   - **Password**: كلمة مرور قوية (8 أحرف على الأقل)
7. اضغط **Add user**
8. **انسخ UID** للمستخدم (سيكون مثل: `abc123def456...`)

### الخطوة 2: إضافة المستخدم في Firestore

1. في Firebase Console، اضغط على **Firestore Database**
2. اضغط على **Start collection** (إذا لم يكن لديك collection `admins`)
3. Collection ID: `admins`
4. Document ID: **الصق UID** الذي نسخته في الخطوة السابقة
5. أضف الحقول التالية:

```json
{
  "email": "admin@valentino.com",
  "displayName": "المدير",
  "role": "super-admin",
  "createdAt": [Timestamp - اضغط على أيقونة الساعة واختر الآن]
}
```

6. اضغط **Save**

### الخطوة 3: تسجيل الدخول

1. اذهب إلى: `http://localhost:5173/admin/login` (أو رابط الموقع)
2. أدخل:
   - **Email**: البريد الذي أنشأته
   - **Password**: كلمة المرور
3. اضغط **تسجيل الدخول**

## ✅ التحقق

بعد تسجيل الدخول بنجاح، يجب أن:
- ✅ يتم توجيهك إلى `/admin`
- ✅ ترى لوحة التحكم
- ✅ يمكنك الوصول إلى جميع الصفحات الإدارية

## 🔧 استكشاف الأخطاء

### المشكلة: "ليس لديك صلاحيات الوصول"
**الحل:**
- تأكد من أنك أضفت المستخدم في collection `admins` في Firestore
- تأكد من أن Document ID = UID المستخدم
- تأكد من أن الحقول موجودة (email, displayName, role)

### المشكلة: "البريد الإلكتروني أو كلمة المرور غير صحيحة"
**الحل:**
- تأكد من أنك أنشأت المستخدم في Firebase Authentication
- تأكد من صحة البريد وكلمة المرور

### المشكلة: "حدث خطأ أثناء تسجيل الدخول"
**الحل:**
- تحقق من أن Firebase متصل بشكل صحيح
- تحقق من console المتصفح للأخطاء
- تأكد من أن `.env` يحتوي على بيانات Firebase الصحيحة

---

## 📝 ملاحظات مهمة

- ⚠️ يمكنك إنشاء عدة مستخدمين Admin
- ⚠️ Document ID في Firestore يجب أن يكون UID المستخدم بالضبط
- ⚠️ الحقل `role` يمكن أن يكون `super-admin` أو `admin`
- ⚠️ تأكد من أن كلمة المرور قوية (8 أحرف على الأقل)

