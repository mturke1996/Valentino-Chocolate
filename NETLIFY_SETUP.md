# إعداد Netlify للموقع

## خطوات إضافة متغيرات البيئة في Netlify

### 1. اذهب إلى Netlify Dashboard

- افتح موقعك على [Netlify](https://app.netlify.com)
- اختر موقعك من القائمة

### 2. إضافة متغيرات البيئة

1. اذهب إلى **Site settings** (إعدادات الموقع)
2. اختر **Environment variables** (متغيرات البيئة) من القائمة الجانبية
3. اضغط على **Add variable** (إضافة متغير)

### 3. أضف المتغيرات التالية (من Firebase Console):

#### كيفية الحصول على قيم Firebase:

1. اذهب إلى [Firebase Console](https://console.firebase.google.com)
2. اختر مشروعك
3. اضغط على ⚙️ **Project Settings** (إعدادات المشروع)
4. في تبويب **General**، ابحث عن قسم **Your apps**
5. إذا لم يكن لديك تطبيق ويب، اضغط **Add app** واختر **Web** (</>)
6. انسخ القيم التالية:

#### المتغيرات المطلوبة:

```
VITE_FIREBASE_API_KEY=AIzaSy... (قيمة apiKey)
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789 (رقم)
VITE_FIREBASE_APP_ID=1:123456789:web:abc123 (قيمة appId)
```

### 4. بعد إضافة جميع المتغيرات:

1. اضغط **Save** (حفظ)
2. اذهب إلى **Deploys** (النشرات)
3. اضغط على **Trigger deploy** > **Clear cache and deploy site** (إعادة نشر مع مسح الذاكرة المؤقتة)

### 5. انتظر حتى يكتمل البناء

- سيتم إعادة بناء الموقع تلقائياً
- بعد اكتمال البناء، يجب أن يعمل الموقع بشكل صحيح

## ملاحظات مهمة:

- ⚠️ **لا تشارك** متغيرات البيئة مع أي شخص
- ✅ تأكد من إضافة جميع المتغيرات الستة
- 🔄 بعد إضافة المتغيرات، يجب إعادة نشر الموقع
- 📝 يمكنك إضافة متغيرات أخرى (ImgBB, Telegram) إذا كنت تستخدمها

## حل المشاكل:

### إذا ظهرت رسالة خطأ "Missing environment variables":

1. تأكد من إضافة جميع المتغيرات الستة
2. تأكد من كتابة الأسماء بشكل صحيح (بما في ذلك `VITE_` في البداية)
3. تأكد من عدم وجود مسافات إضافية في القيم
4. أعد نشر الموقع بعد إضافة المتغيرات

### إذا لم يعمل Firebase بعد إضافة المتغيرات:

1. تحقق من أن القيم صحيحة من Firebase Console
2. تأكد من أن المشروع نشط في Firebase
3. تحقق من قواعد Firestore و Storage في Firebase Console

