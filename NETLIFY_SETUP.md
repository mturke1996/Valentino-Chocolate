# 🚀 دليل إعداد Netlify

## ⚠️ المشكلة الحالية

الموقع يظهر شاشة بيضاء على Netlify بسبب عدم وجود متغيرات البيئة (Environment Variables) الخاصة بـ Firebase.

## ✅ الحل: إضافة متغيرات البيئة على Netlify

### الخطوة 1: الحصول على بيانات Firebase

1. اذهب إلى [Firebase Console](https://console.firebase.google.com/)
2. اختر مشروعك
3. اضغط على ⚙️ (الإعدادات) → **Project Settings**
4. انزل إلى قسم **"Your apps"**
5. انسخ بيانات `firebaseConfig`

### الخطوة 2: إضافة المتغيرات على Netlify

1. اذهب إلى [Netlify Dashboard](https://app.netlify.com/)
2. اختر موقعك (Valentino-Chocolate)
3. اضغط على **Site settings** (إعدادات الموقع)
4. من القائمة الجانبية، اضغط على **Environment variables** (متغيرات البيئة)
5. اضغط على **Add a variable** (إضافة متغير)

6. أضف المتغيرات التالية واحدة تلو الأخرى:

```
VITE_FIREBASE_API_KEY = AIzaSyXXXXXXXXXXXXXXXXXXXX
VITE_FIREBASE_AUTH_DOMAIN = your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID = your-project-id
VITE_FIREBASE_STORAGE_BUCKET = your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID = 123456789012
VITE_FIREBASE_APP_ID = 1:123456789012:web:xxxxxxxxxxxxx
```

**⚠️ مهم جداً:** استبدل القيم بقيمك الحقيقية من Firebase!

### الخطوة 3: إضافة متغيرات اختيارية (إن وجدت)

إذا كنت تستخدم ImgBB أو Telegram:

```
VITE_IMGBB_API_KEY = your_imgbb_api_key_here
VITE_TELEGRAM_BOT_TOKEN = your_telegram_bot_token
VITE_TELEGRAM_CHAT_ID = your_telegram_chat_id
```

### الخطوة 4: إعادة النشر

بعد إضافة جميع المتغيرات:

1. اذهب إلى **Deploys** (النشرات)
2. اضغط على **Trigger deploy** → **Deploy site**
3. أو انتظر حتى يتم النشر تلقائياً بعد push جديد

## 📋 قائمة التحقق

- [ ] تم إضافة `VITE_FIREBASE_API_KEY`
- [ ] تم إضافة `VITE_FIREBASE_AUTH_DOMAIN`
- [ ] تم إضافة `VITE_FIREBASE_PROJECT_ID`
- [ ] تم إضافة `VITE_FIREBASE_STORAGE_BUCKET`
- [ ] تم إضافة `VITE_FIREBASE_MESSAGING_SENDER_ID`
- [ ] تم إضافة `VITE_FIREBASE_APP_ID`
- [ ] تم إعادة النشر

## 🔍 التحقق من النشر

بعد إعادة النشر، افتح الموقع وتحقق من:
- ✅ الموقع يعمل بدون شاشة بيضاء
- ✅ يمكن رؤية المنتجات
- ✅ يمكن تسجيل الدخول للوحة التحكم

## ❓ مشاكل شائعة

### المشكلة: لا تزال الشاشة بيضاء
**الحل:** 
- تأكد من إضافة جميع المتغيرات الـ 6
- تأكد من عدم وجود مسافات إضافية في القيم
- تأكد من إعادة النشر بعد إضافة المتغيرات

### المشكلة: خطأ "invalid-api-key"
**الحل:**
- تأكد من نسخ `apiKey` بشكل صحيح من Firebase
- تأكد من أن المتغير يبدأ بـ `VITE_`

### المشكلة: الموقع يعمل محلياً لكن لا يعمل على Netlify
**الحل:**
- هذا طبيعي! ملف `.env` يعمل محلياً فقط
- يجب إضافة المتغيرات على Netlify كما هو موضح أعلاه

---

## 📸 لقطات شاشة

### 1. موقع Environment Variables في Netlify:
```
Site settings → Environment variables → Add a variable
```

### 2. مثال على إضافة متغير:
```
Key: VITE_FIREBASE_API_KEY
Value: AIzaSyXXXXXXXXXXXXXXXXXXXX
```

---

## 🎉 بعد الإعداد

بعد إضافة جميع المتغيرات وإعادة النشر، الموقع سيعمل بشكل مثالي! 🚀

