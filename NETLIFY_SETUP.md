# 🔧 إعداد Firebase على Netlify

## المشكلة
إذا كانت قاعدة البيانات Firebase لا تعمل على Netlify، السبب عادة هو عدم إضافة متغيرات البيئة.

## الحل: إضافة متغيرات البيئة في Netlify

### الخطوات:

1. **اذهب إلى Netlify Dashboard**
   - افتح [Netlify Dashboard](https://app.netlify.com/)
   - اختر موقعك

2. **افتح إعدادات الموقع**
   - اضغط على **Site settings**
   - من القائمة الجانبية، اضغط على **Environment variables**

3. **أضف المتغيرات التالية:**

   اضغط على **Add variable** وأضف كل متغير على حدة:

   ```
   VITE_FIREBASE_API_KEY = your_api_key_here
   VITE_FIREBASE_AUTH_DOMAIN = your_project.firebaseapp.com
   VITE_FIREBASE_PROJECT_ID = your_project_id
   VITE_FIREBASE_STORAGE_BUCKET = your_project.appspot.com
   VITE_FIREBASE_MESSAGING_SENDER_ID = your_sender_id
   VITE_FIREBASE_APP_ID = your_app_id
   ```

4. **كيفية الحصول على هذه القيم:**

   - اذهب إلى [Firebase Console](https://console.firebase.google.com/)
   - اختر مشروعك
   - اضغط على ⚙️ **Project settings**
   - في قسم **Your apps**، اختر تطبيق الويب أو أنشئ واحداً جديداً
   - انسخ القيم من `firebaseConfig`

5. **إعادة النشر**
   - بعد إضافة المتغيرات، اذهب إلى **Deploys**
   - اضغط على **Trigger deploy** → **Deploy site**
   - أو ادفع تغييرات جديدة إلى GitHub

## ✅ التحقق من أن كل شيء يعمل

بعد إعادة النشر، افتح الموقع وتحقق من:
- ✅ Console المتصفح لا تظهر أخطاء Firebase
- ✅ يمكنك تسجيل الدخول إلى لوحة التحكم
- ✅ يمكنك عرض المنتجات
- ✅ قاعدة البيانات تعمل

## 🔍 استكشاف الأخطاء

### المشكلة: "Firebase configuration is invalid"
**الحل:** تأكد من إضافة جميع المتغيرات الستة في Netlify

### المشكلة: "Missing or invalid Firebase environment variables"
**الحل:** تحقق من أن القيم لا تحتوي على "XXXX" أو "your-" أو "demo-"

### المشكلة: قاعدة البيانات لا تعمل بعد إضافة المتغيرات
**الحل:** 
- تأكد من إعادة النشر بعد إضافة المتغيرات
- تحقق من Console المتصفح للأخطاء
- تأكد من أن Firestore Rules صحيحة

---

**ملاحظة مهمة:** متغيرات البيئة في Netlify حساسة لحالة الأحرف. تأكد من كتابتها بالضبط كما هو موضح أعلاه.

